#include <iostream>

using namespace std;

int main(int argc, char const *argv[])
{
  int A, B, C, D, diferenca;
  cin >> A >> B >> C >> D;

  diferenca = (A*B)-(C*D);

  cout << "DIFERENCA = " << diferenca << endl;
  return 0;
}
